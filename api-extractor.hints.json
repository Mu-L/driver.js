{
  "$schema": "https://developer.microsoft.com/json-schemas/api-extractor/v7/api-extractor.schema.json",

  "projectFolder": ".",
  "mainEntryPointFilePath": "<projectFolder>/dist-types/hints.d.ts",

  "compiler": {
    "tsconfigFilePath": "<projectFolder>/tsconfig.json"
  },

  "dtsRollup": {
    "enabled": true,
    "untrimmedFilePath": "<projectFolder>/dist/hints.d.ts"
  },

  "docModel": { "enabled": false },
  "apiReport": { "enabled": false },
  "tsdocMetadata": { "enabled": false },

  "messages": {
    "compilerMessageReporting": {
      "default": { "logLevel": "none" }
    },
    "extractorMessageReporting": {
      "default": { "logLevel": "warning" },
      // The package documents its API in the docs site rather than with TSDoc
      // release tags, so every export would otherwise warn.
      "ae-missing-release-tag": { "logLevel": "none" }
    }
  }
}
